{
  "name": "peeps",
  "version": "1.0.0",
  "description": "Family-centered social platform",
  "private": true,
  "scripts": {
    "dev": "turbo run dev --filter=@peeps/web",
    "dev:web": "turbo run dev --filter=@peeps/web",
    "dev:mobile": "turbo run dev --filter=@peeps/mobile",
    "dev:all": "turbo run dev --filter=@peeps/web --filter=@peeps/mobile",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "turbo": "latest",
    "prettier": "^3.1.0"
  },
  "packageManager": "pnpm@8.0.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
